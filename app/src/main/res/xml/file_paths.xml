<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Ubuntu rootfs and everything else in the app's private files dir. -->
    <files-path name="files" path="." />
    <external-files-path name="external" path="." />
    <external-path name="sdcard" path="." />
</paths>
